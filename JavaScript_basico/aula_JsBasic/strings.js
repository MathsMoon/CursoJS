/* Trabalhando com Strings:

let frase = "Frase perfeita para testar coisas";

frase.length(); -> Tamanho da String em caracteres.
frase.slice(); -> corta a string de acordo com os parâmetros colocados dentro do ().
frase.trim(); -> remove os espaços indentados dentro da String.
frase.padStart(); -> vai preenchendo até chegar o número colocado no pad. ex: padStart(2, 0) -> 001;
frase.repeat(); -> repete a string um determinado número de vezes.
frase.replace(); -> troca uma string por outra, a função busca a string a ser substituída e troca.
*/