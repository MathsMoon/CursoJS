//Dá para trocar o nome das variáveis que chegam utilizando o as.
//O dataList é a função padrão do módulo.
import dataList, { data as json_data} from "./module";

//criando uma lista apenas com os nomes dos JSONs.
dataList(json_data);